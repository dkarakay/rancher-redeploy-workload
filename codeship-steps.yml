- type: parallel
  name: parallel_steps
  steps:
    - service: rancher_build
      command: printenv
    - service: rancher_build
- name: tagged_step
  tag: master
  service: rancher_build
  command: echo "Deploy here!"
